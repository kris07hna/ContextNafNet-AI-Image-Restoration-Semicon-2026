# Split Manifests

Frozen, versioned train/validation manifests belong here. Each manifest records sample names, grouping algorithm and version, thresholds, seed, dataset hashes, and summary distributions.
